<%- include('../_layout', { title: title }) %>

<div class="container mx-auto p-4">
  <h1 class="text-2xl font-bold">Accetta invito</h1>
  <form action="/accept-invite" method="post">
    <input type="hidden" name="token" value="<%= token %>" />
    <p>Clicca per completare la registrazione e configurare MFA.</p>
    <button class="btn btn-primary" type="submit">Accetta invito</button>
  </form>
</div>
